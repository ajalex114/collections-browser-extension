{
  "name": "collection-extension",
  "version": "1.0.0",
  "description": "Collection — an MV3 browser extension reimplementing Edge Collections. The loadable extension lives in src/; these package fields exist only for the dev test suite.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
